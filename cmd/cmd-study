free 查看内存使用 -m
exit 退出终端
cd - 返回上一次的目录
file 文件名     （ 简单介绍这个文件）
less 重后向前 看文件
ln -s 源文件 目标文件。（当我们需要在不同的目录，用到相同的文件时，我们不需要在每一个需要的目录下都放一个必须相同的文件，link）
type 命令其实不能算查找命令，它是用来区分某个命令到底是由shell自带的，还是由shell外部的独立二进制文件提供的


############
 watch -d -n 1 'who'   #每隔一秒执行一次who命令，以监视服务器当前用户登录的状况
 watch -d -n 1 'cat log.log'   #每隔一秒执行一次cat log.log命令